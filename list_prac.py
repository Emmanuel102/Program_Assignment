city_names = ["Oakland", "Seattle", "Atlanta", "New York City", "Memphis", "Miami", "Los Angeles", "New Orleans"]
#                0          1          2            3






def printCityNames ():
    counter = 0
    for len(city_names)
    while counter < len(city_names):
        print(city_names[counter])
        counter += 12
        return "completed"


        printCityNames(city_names)



#[longer names ... shorter names]
# getthe length of listitem1, listitem2
check if len[list item 1] > len[next list item 2]
move on
else
remove listitem
put list item at end
#return list!

      
#return new list 
 
        def organize_cities(cities_list):
            print(len(cities_list[0]))
    for len(city_names)
if(len(cities_list[counter])) > len(cities_list[counter + 1])):
    print()
    counter+=1
    else:
        cities_list.remove(city)
        cities_list.append(city)
        counter +=1
organize_cities(c)